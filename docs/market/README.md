# 插件市场收录（awesome-dsh-plugin）

`awesome-dsh-plugin-entry.yml` 是按 awesome-dsh-plugin 贡献指南准备的投稿条目（一个文件即全部投稿）。

**当前状态：前置全部就绪（2026-09-21）**：

1. **可安装路径** ✅：npm 包 `@openbkn/dsh-business-context@0.1.5-rc.1` 已发布（`repository` 字段指回本仓库，收录自动关联下载量）；Release `openbkn-dsh-runtime-v0.1.6-alpha.2-openbkn.1` 亦附独立的 `openbkn-dsh-business-context-0.1.5-rc.1.tgz`，条目已加 `tarball:` 直链。
2. **GitHub topic** ✅：`dsh-plugin` 已加到 openbkn-ai/bkn-dsh。

另请知悉：插件依赖 compat 兼容系列（typert 外部协议识别、ignorable 会话事件写入侧、发布锁文件对），**需配合 OpenBKN Runtime 使用**——描述中已如实注明；DSH 侧修复分支存于 kalias/deepseek-harness（上游 GitHub 为只读镜像、issues/PR 均禁用，通道打开即可贡献）。
